{
  "items": [
    {
      "id": "js-fetch-json",
      "title": "Fetch JSON with async/await",
      "language": "javascript",
      "tags": ["fetch", "api", "json"],
      "summary": "Basic async fetch flow that returns parsed JSON and throws on HTTP errors.",
      "code": "async function getJson(url) {\n  const response = await fetch(url);\n\n  if (!response.ok) {\n    throw new Error(`Request failed: ${response.status}`);\n  }\n\n  return response.json();\n}"
    },
    {
      "id": "js-debounce",
      "title": "Debounce a function",
      "language": "javascript",
      "tags": ["performance", "events", "utility"],
      "summary": "Delay execution until calls stop happening for the configured wait window.",
      "code": "function debounce(fn, wait = 250) {\n  let timeoutId;\n\n  return function debounced(...args) {\n    clearTimeout(timeoutId);\n    timeoutId = setTimeout(() => fn.apply(this, args), wait);\n  };\n}"
    }
  ]
}
